/**
 * GroupTheme — Design System INLOG
 *
 * Ce fichier est le point d'entrée du thème.
 * Le preset est généré par le PrimeNG ThemeDesigner et stocké dans ./inlog/
 *
 * Pour mettre à jour le thème :
 * 1. Exporter le nouveau preset depuis ThemeDesigner
 * 2. Remplacer les fichiers dans src/app/theme/inlog/
 * 3. Relancer ng serve — le thème s'applique automatiquement
 */
import InlogPreset from './inlog/index';

export const GroupTheme = InlogPreset;
